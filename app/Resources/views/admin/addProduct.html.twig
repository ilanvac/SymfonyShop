{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        {{ form_start(form,{'attr':{'id':'ProductForm','action':url('product'),'novalidate': 'novalidate'}}) }}
        {{ form_row(form, {'attr':{'class':'form-group' }}) }}
        {{ form_end(form) }}
    </div>
{% endblock %}